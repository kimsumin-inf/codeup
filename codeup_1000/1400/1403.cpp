#include <iostream>
using namespace std;

int main() {
	int n,cnt=0;
	cin >> n;
	if (n > 1000 || n<1)
		cin >> n;
	int* arr = new int[n];
	for (int i = 0; i < n; i++) {

		cin>>arr[i];
	}

	for (int )
	delete[] arr;

}